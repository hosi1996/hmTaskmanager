# تصمیم‌های معماری (ADR کوتاه)

| # | تصمیم | دلیل |
|---|-------|------|
| 1 | Fastify + Prisma (JavaScript ESM، بدون مرحله‌ی build) | سبک، سریع، کانتینر کوچک؛ مهاجرت به TypeScript بعداً ممکن است |
| 2 | SvelteKit (SPA با `ssr=false`) + Tailwind 4 + adapter-node | باندل کوچک؛ پنل پشت لاگین است و SEO نیاز ندارد |
| 3 | نشست سمت سرور در Redis (کوکی httpOnly + SameSite=Lax)، نه JWT | امکان «خروج از راه دور» و قفل فوری؛ CSRF با هدر سفارشی `x-requested-with` |
| 4 | هش رمز با argon2id (`@node-rs/argon2`) | باینری آماده برای Alpine (musl) |
| 5 | 2FA (TOTP) با پیاده‌سازی داخلی RFC 6238 | بدون وابستگی اضافه |
| 6 | ایزوله‌سازی دسترسی در `lib/access.js`: `visibleTasksWhere()` فیلتر Prisma می‌سازد | مشتری هیچ‌وقت ردیف غیرمجاز را از دیتابیس نمی‌گیرد (نه فقط UI) |
| 7 | بات تلگرام = سرویس مجزا با long-polling؛ فقط به API داخلی `/internal/bot/*` (توکن مشترک) وصل است | نیازی به وب‌هوک/گواهی نیست؛ `/internal` از Nginx عبور داده نمی‌شود. دانلود فایل و ارسال پیام تلگرام در بک‌اند انجام می‌شود |
| 8 | PostgreSQL و Nginx موجود روی سرور استفاده می‌شوند؛ شبکه‌ی داکر `hmtm-net` مجزا | عدم تداخل با سرویس‌های دیگر |
| 9 | شناسه‌ها UUID v4 (`@default(uuid())`)؛ نام فایل‌های آپلودی = UUID خارج از مسیر public | الزام پروژه |
| 10 | هر دامنه‌ی بک‌اند یک پوشه‌ی `modules/<name>/routes.js` (فرانت: `lib/` + `routes/`) | افزودن قابلیت بدون دست‌زدن به بخش‌های قبلی |

## نقش‌ها
- **سیستمی:** OWNER (همه‌چیز) / STAFF / CLIENT
- **پروژه:** VIEWER < REPORTER < CONTRIBUTOR < MANAGER
- Viewer/Reporter (و هر مشتری غیر مدیر) فقط تسک‌هایی را می‌بینند که سازنده/اساین/دنبال‌کننده‌شان هستند. Contributor کارمند و Manager همه‌ی تسک‌های پروژه را می‌بینند.

## مسیر توسعه‌ی بعدی
Webhook خروجی/API عمومی (توکن API در جدول جدید + همان `access.js`)، PWA، اعلان ایمیلی (SMTP)، جستجوی کامل‌تر با `pg_trgm`/tsvector.

## ماژول مانیتورینگ دامنه
پروژه‌ی جدای `uptimebot` (پایتون + python-telegram-bot) به‌جای اینکه به‌صورت سرویس مجزا کنار سیستم اجرا شود، به‌عنوان یک ماژول داخل خودِ بک‌اند (`modules/monitoring`) بازنویسی شد:
- **چک‌ها** عیناً پیاده‌سازی شدند (DNS/NS/MX با `node:dns`، Ping با اجرای باینری سیستم، SSL با `node:tls`، HTTP/HTTPS/ریدایرکت/سرعت با `fetch`، انقضای دامنه با RDAP)؛ `iputils` به Dockerfile بک‌اند اضافه شد تا `ping` در دسترس باشد.
- هر دامنه به یک **پروژه** وصل است، نه یک چت تلگرام مستقل؛ گزارش مشکل به همان گروه تلگرامی می‌رود که پروژه به آن وصل است (از زیرساخت موجود `lib/notify.js`/`tgSend` استفاده شد) و هم‌زمان یک نوتیفیکیشن داخل پنل برای مدیران پروژه ساخته می‌شود.
- زمان‌بندی با یک تیک هر ۲۰ ثانیه (`modules/monitoring/scheduler.js`) و قفل Redis مشابه `scheduler.js` موجود انجام می‌شود؛ هر دامنه بازه‌ی زمانی مستقل خودش را دارد.
- «محل چک» (خارج/ایران/هر دو) دقیقاً مثل ربات اصلی، از طریق همان `iran-checker/check.php` (بدون تغییر، فقط کپی‌شده) با یک URL/Token قابل‌تنظیم در پنل مدیریت انجام می‌شود؛ اگر تنظیم نشود، آن گزینه غیرفعال می‌ماند.
- دسترسی مدیریت دامنه‌ها فقط برای MANAGER/OWNER پروژه است (نه مشتری)، چون داده‌ی فنی داخلی است، نه چیزی که مشتری باید ببیند.
- **نتایج به‌صورت آرایه ذخیره می‌شوند، نه شیء کلیدشده با نام چک.** وقتی محل چک «هر دو» است، همان چک هم از این سرور و هم از ایران اجرا می‌شود؛ اگر نتیجه در یک شیء با کلید نام چک ذخیره شود، نتیجه‌ی دوم اولی را پاک می‌کند و کاربر فقط یکی از دو محل را می‌بیند (باگی که در نسخه‌ی اول این ماژول وجود داشت و اصلاح شد). فرانت‌اند (`monitorRows()` در `format.js`) هم فرمت آرایه و هم فرمت قدیمی شیء را می‌پذیرد تا لاگ‌های قدیمی خراب نشوند.
- **چک‌های «کلمه‌ی کلیدی در صفحه»** (تشخیص خرابی/دیفیس با وجودنداشتن یک متن مشخص) **و «پورت سفارشی»** (اتصال TCP به یک پورت دلخواه، برای سرویس‌های غیر HTTP) اضافه شدند؛ این دو همیشه از همین سرور اجرا می‌شوند چون `iran-checker/check.php` آن‌ها را پشتیبانی نمی‌کند.
- چک SSL حالا نسخه‌ی TLS مذاکره‌شده را هم در جزئیات نشان می‌دهد و اگر TLSv1/TLSv1.1 (قدیمی و ناامن) باشد، مشکل گزارش می‌کند.
- هر دامنه یک `logRetentionDays` مستقل دارد (۱ روز تا ۳ ماه، یا هرگز)؛ یک تیک ساعتی در `scheduler.js` لاگ‌های قدیمی‌تر را پاک می‌کند، و یک دکمه‌ی «پاک‌سازی الان» هم برای اجرای فوری همان قاعده هست.
